"use client";

const AddToCard = () => {
  return (
    <div>
      <button className="btn btn-primary" onClick={() => console.log("click")}>
        Click
      </button>
    </div>
  );
};

export default AddToCard;
